
#include <iostream>
#include <cmath>

int main(void) {
	int t;
	std::cin >> t;
	for (int i = 0; i < t; i++) {
		// int n;
		// long long max;
		// long long sub;
		// std::cin >> n;
		// max = 2 * n;
		// if (n == 1) {

		// } else if (n % 2) {
		// 	sub = (n + 1) * (n / 2) + (n / 2);
		// } else {
		// 	sub = (n + 1) * (n / 2);
		// }
		// std::cout << max - sub << '\n';
		std::cout < << std::endl;
	}
}
